# ============================================================
# TemplaFill Backend — Python Dependencies
# Install: pip install -r requirements.txt
#
# VULN-13: direct dependencies are pinned to exact, tested versions so builds
# are reproducible and cannot silently pull a vulnerable/broken release.
# Run `pip-audit -r requirements.txt` before upgrading.
# ============================================================

# ----- Core Framework -----
fastapi==0.141.1
uvicorn[standard]==0.46.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.31
# (pinned to a CVE-fixed release; 0.0.28 had PYSEC-2026-3036/3037/3040)
python-dotenv==1.2.3

# ----- PDF Processing -----
PyMuPDF==1.28.2
pdfplumber==0.11.10

# ----- Template Processing -----
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2

# ----- RAG / LLM -----
# Optional integrations: pinned to the current major line (compatible release).
google-genai~=1.0
pgvector~=0.3
sqlalchemy[asyncio]~=2.0
asyncpg~=0.29
alembic~=1.13

# ----- Task Queue -----
celery~=5.3
redis~=5.0

# ----- Testing -----
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
pytest-cov~=5.0
reportlab==4.4.10  # test fixture generation (table PDFs)
